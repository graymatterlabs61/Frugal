---
phase: 04-budget-rules-api-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/tier.ts
autonomous: true
requirements:
  - F-14
  - F-21

must_haves:
  truths:
    - "canCreateBudgetRules returns false only for plan='free'"
    - "canUseThrottle returns true only for plan='pro'"
    - "Tier functions check actual DB plan values (free/starter/growth/pro), not UI labels (plus)"
  artifacts:
    - path: "lib/tier.ts"
      provides: "Feature gate functions for budget rule tier enforcement"
      exports: ["canCreateBudgetRules", "canUseThrottle", "canUseBlock"]
  key_links:
    - from: "lib/tier.ts"
      to: "app/api/budget-rules/route.ts"
      via: "import { canCreateBudgetRules, canUseThrottle } from '@/lib/tier'"
      pattern: "canCreateBudgetRules|canUseThrottle"
    - from: "lib/tier.ts"
      to: "app/(dashboard)/projects/[id]/ProjectDetailClient.tsx"
      via: "userPlan prop compared against tier gates"
      pattern: "canCreateBudgetRules|canUseThrottle"
---

<objective>
Create lib/tier.ts — a new file with pure functions that map users.plan DB values to feature capability booleans. This file is imported by both the API routes (server-side enforcement) and the UI (client-side gating display).

Purpose: Single source of truth for tier gating. DB plan values (free/starter/growth/pro) differ from UI labels (free/plus/pro) — this file bridges them.
Output: lib/tier.ts with three exported functions.
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-budget-rules-api-ui/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lib/tier.ts with feature gate functions</name>
  <files>lib/tier.ts</files>
  <action>
Create `lib/tier.ts` as a new file. This file has NO imports — it is pure functions only.

```typescript
/**
 * lib/tier.ts — Feature gate functions for Frugal plan tiers.
 *
 * DB plan values: 'free' | 'starter' | 'growth' | 'pro'
 *
 * UI shows 'Plus' for starter/growth, 'Pro' for pro.
 * These functions always check DB values — never UI labels.
 */

/**
 * Returns true if the user's plan allows creating budget rules.
 * Free users cannot create rules; all paid plans (starter, growth, pro) can.
 */
export function canCreateBudgetRules(plan: string): boolean {
  return plan !== "free";
}

/**
 * Returns true if the user's plan allows the "block" action on rules.
 * Requires any paid plan (starter, growth, or pro).
 */
export function canUseBlock(plan: string): boolean {
  return plan !== "free";
}

/**
 * Returns true if the user's plan allows the "throttle" action on rules.
 * Throttle is Pro-only — requires plan === 'pro'.
 */
export function canUseThrottle(plan: string): boolean {
  return plan === "pro";
}
```

Important:
- No `any` types.
- No imports needed — pure functions only.
- `canUseBlock` and `canCreateBudgetRules` use the same logic (`plan !== 'free'`) but are separate exports for clarity — each may diverge in future phases.
- `canUseThrottle` strictly checks `=== 'pro'` — starter/growth do NOT get throttle.
  </action>
  <verify>
Run: `npx tsc --noEmit`
Expected: exits 0 (no TypeScript errors).

Also verify:
- lib/tier.ts exports `canCreateBudgetRules`, `canUseBlock`, `canUseThrottle`
- No import statements in the file
- Functions accept `string` param, return `boolean`
  </verify>
  <done>
lib/tier.ts exists with three exported functions. canCreateBudgetRules('free') returns false, canCreateBudgetRules('starter') returns true, canUseThrottle('growth') returns false, canUseThrottle('pro') returns true. TypeScript compiles clean.
  </done>
</task>

</tasks>

<verification>
- lib/tier.ts exists
- Three functions exported: canCreateBudgetRules, canUseBlock, canUseThrottle
- `npx tsc --noEmit` exits 0
- Logic uses DB values (free/starter/growth/pro), not UI labels
</verification>

<success_criteria>
lib/tier.ts is created with correct tier gate logic. Can be imported by API routes and UI components in plans 03 and 04.
</success_criteria>

<output>
After completion, create `.planning/phases/04-budget-rules-api-ui/04-02-SUMMARY.md`
</output>
