---
phase: 03-dashboard-real-data
plan: 04
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - app/(dashboard)/projects/[id]/page.tsx
  - app/(dashboard)/projects/[id]/ProjectDetailClient.tsx
  - lib/polling/worker.ts
autonomous: true
requirements:
  - F-18

must_haves:
  truths:
    - "Project detail page loads real project data by UUID param — no hardcoded dict lookup"
    - "Summary stat cards show real monthlySpend, burnRateDaily, projectedMonthly from DB"
    - "Connections tab shows real api_connections with last_polled_at as relative time"
    - "Alerts tab shows real alert_log rows scoped to the project"
    - "worker.ts uses .eq('status', 'active') instead of .eq('is_active', true)"
    - "Navigating to a project UUID that doesn't belong to the user returns a 404-style error state"
  artifacts:
    - path: "app/(dashboard)/projects/[id]/page.tsx"
      provides: "Async server component fetching project data by UUID, passing typed props to client"
    - path: "app/(dashboard)/projects/[id]/ProjectDetailClient.tsx"
      provides: "Client component with tabs, state for delete actions, receives typed props"
    - path: "lib/polling/worker.ts"
      provides: "Bug fixed: .eq('status', 'active') on line 16"
  key_links:
    - from: "app/(dashboard)/projects/[id]/page.tsx"
      to: "lib/queries/dashboard.ts"
      via: "getProjectStats, getProjectConnections, getProjectAlerts"
      pattern: "getProjectStats|getProjectConnections|getProjectAlerts"
    - from: "app/(dashboard)/projects/[id]/page.tsx"
      to: "app/(dashboard)/projects/[id]/ProjectDetailClient.tsx"
      via: "props: ProjectStats, ProjectConnection[], ProjectAlert[]"
      pattern: "ProjectDetailClient"
---

<objective>
Split the project detail page into server + client components, wire all tabs to real data, and fix the worker.ts bug.

Purpose: Real projects have UUID ids — the current dict lookup always returns undefined. Splitting into server + client follows the research finding and enables proper data fetching. Worker bug fix corrects a silent filter error.
Output: page.tsx (server), ProjectDetailClient.tsx (client), worker.ts (bug fix).
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dashboard-real-data/03-01-SUMMARY.md
@lib/queries/dashboard.ts
@app/(dashboard)/projects/[id]/page.tsx
@lib/polling/worker.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix worker.ts bug and create server component page.tsx</name>
  <files>
    lib/polling/worker.ts
    app/(dashboard)/projects/[id]/page.tsx
  </files>
  <action>
**worker.ts bug fix:**

On line 16, change:
```ts
.eq("is_active", true)
```
to:
```ts
.eq("status", "active")
```
No other changes to worker.ts. Comment above that line: `// Filter to active connections by status column (is_active is synced by trigger but status is canonical)`.

---

**page.tsx — rewrite as async server component:**

The current page.tsx is "use client" with useParams and a string-keyed dict. Replace entirely.

New page.tsx must:
1. NOT have "use client" directive — it is a server component.
2. Accept `params: Promise<{ id: string }>` prop (Next.js 15+ params are async).
3. Auth: `const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser();`. Redirect to /login if no user.
4. Resolve params: `const { id } = await params`.
5. Fetch in parallel:
```ts
const [project, connections, alerts] = await Promise.all([
  getProjectStats(supabase, user.id, id),
  getProjectConnections(supabase, user.id, id),
  getProjectAlerts(supabase, user.id, id, 20),
]);
```
6. If `project === null`, render not-found state:
```tsx
<div className="space-y-4">
  <Link href="/projects" className="inline-flex items-center gap-2 text-sm text-muted-foreground hover:text-foreground transition-colors">
    <ArrowLeft size={16} />
    Back to Projects
  </Link>
  <div className="border border-border rounded-2xl p-12 text-center bg-card">
    <p className="text-muted-foreground">Project not found.</p>
  </div>
</div>
```
7. If project found, import and render `<ProjectDetailClient project={project} connections={connections} alerts={alerts} />`.
8. Imports needed: createClient from @/lib/supabase/server, redirect/Link from next/navigation and next/link, getProjectStats/getProjectConnections/getProjectAlerts/ProjectStats/ProjectConnection/ProjectAlert from @/lib/queries/dashboard, ArrowLeft from @phosphor-icons/react/dist/ssr (server-safe import path), ProjectDetailClient from ./ProjectDetailClient.
</action>
  <verify>
Run `npx tsc --noEmit` — no errors in worker.ts and page.tsx.
</verify>
  <done>worker.ts line 16 uses .eq("status", "active"). page.tsx is a server component that resolves params asynchronously and passes real data to ProjectDetailClient.</done>
</task>

<task type="auto">
  <name>Task 2: Create ProjectDetailClient.tsx with real data rendering</name>
  <files>app/(dashboard)/projects/[id]/ProjectDetailClient.tsx</files>
  <action>
Create app/(dashboard)/projects/[id]/ProjectDetailClient.tsx. This is the "use client" component that receives typed props and handles tab state.

Props interface:
```ts
interface ProjectDetailClientProps {
  project: ProjectStats;
  connections: ProjectConnection[];
  alerts: ProjectAlert[];
}
```

Port ALL the client-side UI from the current page.tsx into this component:
- Breadcrumb + project header (name, status badge, description)
- Summary stat cards (4 cards)
- Tabs (Overview, Connections, Budget Rules, Alerts)

**Summary stat cards — use real data:**
- "Spend This Month": `$${project.monthlySpend.toFixed(2)}` / budget: `${project.budgetLimit !== null ? "$" + project.budgetLimit : "No budget set"}`.
- "Budget Used": If `project.budgetLimit !== null`, compute `pct = Math.round((project.monthlySpend / project.budgetLimit) * 100)`. Else show "—". Progress bar: only render if budgetLimit not null.
- "Burn Rate": `$${project.burnRateDaily.toFixed(2)}` per day avg.
- "Projected / Month": `$${project.projectedMonthly.toFixed(0)}`. Days-until-limit: compute from `(project.budgetLimit - project.monthlySpend) / project.burnRateDaily` if both available and result < 14.

**Project header:**
- `project.name`, `project.description`, `project.status` badge. Remove the `project.provider` badge — ProjectStats doesn't have a single provider (project can have multiple connections). Replace provider badge with `"${connections.length} connection${connections.length !== 1 ? "s" : ""}"` in a subtle style.
- Status styles: keep existing `statusStyles` map for healthy/warning/critical. Map `project.status` from DB: "active" → "healthy", other values keep as-is or default to "healthy".

**Overview tab:**
- SpendChart: import `SpendChart` from `@/components/dashboard/SpendChart`. For project detail, show project-scoped data. Pass an empty `data={[]}` for now — project-scoped chart data requires a separate query beyond this plan's scope. Add a comment: `{/* TODO Phase 3 follow-up: project-scoped chart data */}`. Show a muted "Chart data coming soon" placeholder instead of empty chart. This is acceptable per plan scope.
- Model Breakdown: keep the existing static `modelBreakdown` mock — model breakdown is out of scope for Phase 3 per CONTEXT.md. Add comment: `{/* Mock data — Phase 4 */}`.

**Connections tab — use real data:**
- Iterate over `connections` prop (type `ProjectConnection[]`).
- For each connection:
  - Provider icon: use first 2 chars of `connection.provider.toUpperCase()` (same as current).
  - Key display: `••••••••${connection.apiKeySuffix}`.
  - Status badge: map `connection.status` to `connStatusConfig` entries. Keep existing config.
  - Last polled: format `connection.lastPolledAt` as relative time: compute diff from now in seconds. Show "Xm ago" / "Xh ago" / "Xd ago" / "Just now" (< 60s). If null: "Never polled".
- Delete button: on click, show a toast "To remove a connection, use the Connections page." — do NOT implement delete here (Phase 3 scope: read-only real data).
- If `connections.length === 0`: empty state div "No connections yet."

**Budget Rules tab:** Keep entirely as-is (mock, Phase 4 scope). Keep `AddRuleDialog` and `useState` for rules.

**Alerts tab — use real data:**
- Iterate over `alerts` prop (type `ProjectAlert[]`).
- Severity badge: same styles as existing.
- Message: `alert.message`.
- Time: format `alert.firedAt` as `new Date(alert.firedAt).toLocaleString("en-US", { month: "short", day: "numeric", hour: "2-digit", minute: "2-digit" })`.
- Status text: capitalize `alert.status`.
- If `alerts.length === 0`: empty state "No alerts for this project yet."

**Imports cleanup:** Remove `useParams` (id comes via props now). Keep `useState` for rules tab only. Keep all phosphor icon imports — use the regular client imports (not /dist/ssr).

Export as `export function ProjectDetailClient(...)`.
</action>
  <verify>
Run `npx tsc --noEmit` — no errors in ProjectDetailClient.tsx.
Run `npm run build` — project detail route builds without errors.
</verify>
  <done>ProjectDetailClient.tsx exists, accepts ProjectDetailClientProps, renders real connections with last_polled_at as relative time, renders real alert history. Budget Rules and model breakdown remain mock with TODO comments. TypeScript clean.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` clean across all 3 files.
2. `npm run build` passes for the [id] route.
3. worker.ts line 16: grep confirms `.eq("status", "active")` (not "is_active").
4. page.tsx has no "use client" directive and no hardcoded projectData dict.
5. ProjectDetailClient.tsx has "use client" and receives typed props.
</verification>

<success_criteria>
Project detail page loads real project data by UUID. Connections show real api_connections with relative last_polled_at. Alerts tab shows real project-scoped alert_log rows. worker.ts bug fixed.
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-real-data/03-04-SUMMARY.md`
</output>
