---
phase: 03-dashboard-real-data
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - app/(dashboard)/alerts/page.tsx
autonomous: true
requirements:
  - F-10

must_haves:
  truths:
    - "Alerts page shows real alert_log rows — no hardcoded mock array"
    - "Active / Critical / Resolved stat cards reflect real counts from query results"
    - "Severity badge computed from percent_used (>=100 critical, >=80 warning, else info)"
    - "Alert type label computed from action_taken (block → Budget Limit, else → Budget Threshold)"
    - "Empty state shown when no alerts exist"
  artifacts:
    - path: "app/(dashboard)/alerts/page.tsx"
      provides: "Async server component pulling from alert_log via getRecentAlerts"
  key_links:
    - from: "app/(dashboard)/alerts/page.tsx"
      to: "lib/queries/dashboard.ts"
      via: "getRecentAlerts import"
      pattern: "getRecentAlerts"
---

<objective>
Replace all hardcoded mock data in the alerts page with live alert_log queries.

Purpose: Users see their real budget alert history instead of static placeholder rows. Alerts page becomes a functional record of every threshold crossed.
Output: app/(dashboard)/alerts/page.tsx rewritten as async server component.
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dashboard-real-data/03-01-SUMMARY.md
@lib/queries/dashboard.ts
@app/(dashboard)/alerts/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite alerts/page.tsx as server component with real alert_log data</name>
  <files>app/(dashboard)/alerts/page.tsx</files>
  <action>
Rewrite app/(dashboard)/alerts/page.tsx as an async server component. The existing visual structure is correct — keep it, but replace the hardcoded `alerts` array.

Steps:

1. Remove `"use client"` if present. Make the default export `async`.
2. Import `createClient` from `@/lib/supabase/server`.
3. Import `getRecentAlerts, RecentAlert` from `@/lib/queries/dashboard`.
4. Auth check: `const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser();`. Redirect to `/login` if no user (import redirect from next/navigation).
5. Fetch alerts: `const alerts = await getRecentAlerts(supabase, user.id, 100)` — fetch up to 100 for full history page (not just 5 like on dashboard).

6. The existing interface `Alert` has fields that don't match `RecentAlert` (it had `project`, `provider`, `threshold` which don't exist in alert_log without joins). Remove the old interface and use `RecentAlert` directly.

7. Compute stats from the real `alerts` array:
   - `activeCount`: alerts where `alert.status === "active"` — NOTE: alert_log may not have a meaningful "active" status separate from just existing; map to "active" if not resolved/acknowledged. Use the `status` field directly from `RecentAlert`.
   - `criticalCount`: alerts where `alert.severity === "critical"`.
   - `resolvedCount`: alerts where `alert.status === "resolved"`.

8. Update the alert list rendering to use `RecentAlert` fields:
   - Severity badge: use `alert.severity` — same 3 styles already defined.
   - Status icon: keep `statusIcons` map. `statusStyles` map stays the same.
   - Main text: render `alert.type` as the primary label (e.g. "Budget Threshold") instead of `alert.project`. Then `alert.message` as secondary line.
   - Time: format `alert.firedAt` as absolute datetime: `new Date(alert.firedAt).toLocaleString("en-US", { month: "short", day: "numeric", hour: "2-digit", minute: "2-digit" })` — e.g. "Jun 3, 2:22 PM".
   - Status text: `alert.status` (capitalize first letter: `alert.status.charAt(0).toUpperCase() + alert.status.slice(1)`).
   - Remove the `provider` badge column entirely (not available in alert_log without multi-hop join).
   - Keep `threshold` column removal clean — the `type` field (e.g. "Budget Threshold") already implies it.

9. Empty state: if `alerts.length === 0`, render inside the alert history card div (replacing the divide-y list):
```tsx
<div className="px-5 py-16 text-center">
  <p className="text-sm text-muted-foreground">No alerts yet.</p>
  <p className="text-xs text-muted-foreground/60 mt-1">
    Alerts fire when a budget rule threshold is crossed.
  </p>
</div>
```
Stats cards still show 0 for empty state — no special handling needed.

10. Remove the hardcoded `const alerts: Alert[] = [...]` array entirely.
</action>
  <verify>
Run `npx tsc --noEmit` — no errors in alerts/page.tsx.
Run `npm run build` — no build errors for the alerts route.
</verify>
  <done>alerts/page.tsx has no hardcoded alert array. Real getRecentAlerts call with limit 100. Stats computed from real data. Empty state rendered when alerts.length === 0.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` clean.
2. `npm run build` passes for alerts route.
3. alerts/page.tsx contains no `const alerts = [...]` hardcoded array.
4. Page is an async server component (no "use client").
</verification>

<success_criteria>
Alerts page pulls from real alert_log. Severity, type, and message are computed fields. Provider column removed. Empty state renders for zero alerts.
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-real-data/03-03-SUMMARY.md`
</output>
