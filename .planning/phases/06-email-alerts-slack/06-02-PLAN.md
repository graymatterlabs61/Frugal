---
phase: 06-email-alerts-slack
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - lib/polling/alertService.ts
autonomous: true
requirements:
  - F-15
  - F-17

must_haves:
  truths:
    - "fireAlert() writes delivery_status JSONB to alert_log on every call"
    - "sendEmail() uses RESEND_FROM_ADDRESS env var, falls back to onboarding@resend.dev"
    - "email channel records {sent: true, messageId} on success and {sent: false, error} on failure"
    - "slack channel records {sent: true} on success and {sent: false, error} on failure"
    - "notifiedVia array still populated (backward compatible)"
  artifacts:
    - path: "lib/polling/alertService.ts"
      provides: "Real Resend delivery with delivery_status tracking"
      exports: ["fireAlert"]
  key_links:
    - from: "lib/polling/alertService.ts"
      to: "alert_log"
      via: "supabase.from('alert_log').insert with delivery_status field"
      pattern: "delivery_status"
    - from: "lib/polling/alertService.ts"
      to: "resend.emails.send"
      via: "RESEND_FROM_ADDRESS env var"
      pattern: "RESEND_FROM_ADDRESS"
---

<objective>
Wire alertService.ts to record per-channel delivery outcomes and use the correct Resend from-address.

Purpose: alert_log.delivery_status (added in 06-01) must be written so operators can audit which channel succeeded or failed. The from-address must fall back to onboarding@resend.dev for Resend sandbox accounts before a custom domain is verified.
Output: Updated lib/polling/alertService.ts with delivery_status tracking and RESEND_FROM_ADDRESS support.
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/polling/alertService.ts
@.planning/phases/06-email-alerts-slack/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add delivery_status tracking + RESEND_FROM_ADDRESS to alertService.ts</name>
  <files>lib/polling/alertService.ts</files>
  <action>
    Rewrite alertService.ts with three targeted changes. Preserve all existing logic (sendSlack blocks, buildEmailHtml function, AbortSignal.timeout pattern).

    CHANGE 1 — deliveryStatus object
    In fireAlert(), declare a typed deliveryStatus object BEFORE calling sendEmail/sendSlack:

    ```typescript
    const deliveryStatus: {
      email: { sent: boolean; messageId?: string; error?: string };
      slack: { sent: boolean; error?: string };
    } = {
      email: { sent: false },
      slack: { sent: false },
    };
    ```

    Pass deliveryStatus into sendEmail and sendSlack so they can mutate it.
    Update the alert_log insert to include: delivery_status: deliveryStatus

    CHANGE 2 — sendEmail signature
    Change sendEmail signature to accept deliveryStatus:
    ```typescript
    async function sendEmail(
      payload: AlertPayload,
      notifiedVia: string[],
      deliveryStatus: { email: { sent: boolean; messageId?: string; error?: string } }
    ): Promise<void>
    ```

    Inside sendEmail:
    - On success (no error): set deliveryStatus.email = { sent: true, messageId: data?.id }
    - On error from Resend: set deliveryStatus.email = { sent: false, error: String(error) }; log existing console.error
    - On thrown exception: set deliveryStatus.email = { sent: false, error: String(err) }; log existing console.error

    CHANGE 3 — RESEND_FROM_ADDRESS env var
    Replace the hardcoded from address:
    OLD: `from: "Frugal <alerts@frugal.dev>"`
    NEW: `from: process.env.RESEND_FROM_ADDRESS ?? "Frugal <onboarding@resend.dev>"`

    CHANGE 4 — sendSlack signature
    Change sendSlack signature to accept deliveryStatus:
    ```typescript
    async function sendSlack(
      payload: AlertPayload,
      notifiedVia: string[],
      deliveryStatus: { slack: { sent: boolean; error?: string } }
    ): Promise<void>
    ```

    Inside sendSlack:
    - On res.ok: set deliveryStatus.slack = { sent: true }; push to notifiedVia (existing)
    - On !res.ok: set deliveryStatus.slack = { sent: false, error: `HTTP ${res.status}` }
    - On thrown exception: set deliveryStatus.slack = { sent: false, error: String(err) }; log existing console.error

    No TypeScript `any` types. Keep the existing AlertPayload interface unchanged.
  </action>
  <verify>
    Run: npx tsc --noEmit
    Expected: no type errors in lib/polling/alertService.ts
  </verify>
  <done>
    alertService.ts compiles cleanly. fireAlert() inserts delivery_status JSONB into alert_log. sendEmail uses RESEND_FROM_ADDRESS env var with onboarding@resend.dev fallback. Both sendEmail and sendSlack record outcomes into deliveryStatus.
  </done>
</task>

</tasks>

<verification>
- [ ] npx tsc --noEmit passes with no errors
- [ ] delivery_status included in alert_log insert
- [ ] RESEND_FROM_ADDRESS env var used with fallback to onboarding@resend.dev
- [ ] sendEmail sets deliveryStatus.email.sent = true with messageId on success
- [ ] sendSlack sets deliveryStatus.slack.sent = true on res.ok
- [ ] No TypeScript `any` types introduced
</verification>

<success_criteria>
alertService.ts compiles with no TypeScript errors. On a real alert fire, the alert_log row will contain delivery_status: {email: {sent: true/false, ...}, slack: {sent: true/false, ...}}. Email uses the correct from-address from env.
</success_criteria>

<output>
After completion, create `.planning/phases/06-email-alerts-slack/06-02-SUMMARY.md`
</output>
