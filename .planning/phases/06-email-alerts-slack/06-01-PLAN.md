---
phase: 06-email-alerts-slack
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/005_alerts_slack.sql
autonomous: true
requirements:
  - F-15
  - F-16
  - F-17

must_haves:
  truths:
    - "projects table has a slack_webhook_url text column (nullable)"
    - "alert_log table has a delivery_status jsonb column (nullable)"
    - "Migration file is idempotent — safe to re-run"
  artifacts:
    - path: "supabase/migrations/005_alerts_slack.sql"
      provides: "DDL for slack_webhook_url + delivery_status columns"
      contains: "ALTER TABLE public.projects ADD COLUMN IF NOT EXISTS slack_webhook_url"
  key_links:
    - from: "supabase/migrations/005_alerts_slack.sql"
      to: "lib/polling/budgetChecker.ts"
      via: "projects.slack_webhook_url column referenced in JOIN select"
      pattern: "slack_webhook_url"
    - from: "supabase/migrations/005_alerts_slack.sql"
      to: "lib/polling/alertService.ts"
      via: "alert_log.delivery_status column written on insert"
      pattern: "delivery_status"
---

<objective>
Add two DB columns required for Phase 6 end-to-end alert delivery.

Purpose: alertService.ts needs a delivery_status JSONB column in alert_log to record per-channel outcomes, and budgetChecker.ts needs slack_webhook_url on projects to pass the webhook URL to fireAlert.
Output: supabase/migrations/005_alerts_slack.sql ready to apply.
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@supabase/migrations/004_budget_rules_schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration 005 — slack_webhook_url + delivery_status</name>
  <files>supabase/migrations/005_alerts_slack.sql</files>
  <action>
    Create the SQL migration file. Use ADD COLUMN IF NOT EXISTS for idempotency.

    Schema changes:
    1. projects.slack_webhook_url — text, nullable, no default
       ALTER TABLE public.projects ADD COLUMN IF NOT EXISTS slack_webhook_url text;

    2. alert_log.delivery_status — jsonb, nullable, no default
       ALTER TABLE public.alert_log ADD COLUMN IF NOT EXISTS delivery_status jsonb;

    Add a header comment block matching the style of 004_budget_rules_schema.sql:
    -- Frugal — Migration 005: slack_webhook_url + delivery_status
    -- Run after 004_budget_rules_schema.sql
    -- Adds:
    --   projects.slack_webhook_url text      (nullable — Slack incoming webhook URL per project)
    --   alert_log.delivery_status jsonb      (nullable — {email:{sent,messageId?,error?}, slack:{sent,error?}})

    Do NOT add NOT NULL constraints or defaults — both columns are optional.
    Do NOT create indexes (no query patterns require them at this stage).
    Do NOT touch any other tables.
  </action>
  <verify>
    Review the file content: both ALTER TABLE statements present, IF NOT EXISTS used, no NOT NULL constraints.
  </verify>
  <done>
    005_alerts_slack.sql exists with two idempotent ALTER TABLE statements. File is syntactically valid SQL matching the pattern of 004_budget_rules_schema.sql.
  </done>
</task>

</tasks>

<verification>
- [ ] supabase/migrations/005_alerts_slack.sql exists
- [ ] Contains ALTER TABLE public.projects ADD COLUMN IF NOT EXISTS slack_webhook_url text
- [ ] Contains ALTER TABLE public.alert_log ADD COLUMN IF NOT EXISTS delivery_status jsonb
- [ ] Header comment block present and accurate
- [ ] No NOT NULL or DEFAULT clauses added
</verification>

<success_criteria>
Migration 005 SQL file created and syntactically correct. Applying it to Supabase (via Dashboard SQL editor or `supabase db push`) will add slack_webhook_url to projects and delivery_status to alert_log without error.
</success_criteria>

<output>
After completion, create `.planning/phases/06-email-alerts-slack/06-01-SUMMARY.md`
</output>
