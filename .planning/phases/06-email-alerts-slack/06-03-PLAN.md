---
phase: 06-email-alerts-slack
plan: 03
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - lib/polling/emailTemplates.ts
  - lib/polling/alertService.ts
autonomous: true
requirements:
  - F-15

must_haves:
  truths:
    - "lib/polling/emailTemplates.ts exports buildEmailHtml(payload)"
    - "alertService.ts imports buildEmailHtml from ./emailTemplates (not defined inline)"
    - "Email HTML output is identical to the current inline function"
    - "No duplicate buildEmailHtml definition exists"
  artifacts:
    - path: "lib/polling/emailTemplates.ts"
      provides: "Standalone HTML email builder for budget alerts"
      exports: ["buildEmailHtml"]
    - path: "lib/polling/alertService.ts"
      provides: "Updated import of buildEmailHtml from emailTemplates"
  key_links:
    - from: "lib/polling/alertService.ts"
      to: "lib/polling/emailTemplates.ts"
      via: "import { buildEmailHtml } from './emailTemplates'"
      pattern: "from.*emailTemplates"
---

<objective>
Extract buildEmailHtml into its own module so alertService.ts stays focused on delivery logic.

Purpose: alertService.ts currently has buildEmailHtml defined inline (133 lines). Extracting it makes the template independently testable and easier to edit without touching delivery logic.
Output: lib/polling/emailTemplates.ts with the extracted function; alertService.ts updated to import it.
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/polling/alertService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create emailTemplates.ts and update alertService.ts import</name>
  <files>
    lib/polling/emailTemplates.ts
    lib/polling/alertService.ts
  </files>
  <action>
    STEP 1 — Create lib/polling/emailTemplates.ts

    Define the AlertPayload interface locally (duplicate it — do not import from alertService to avoid circular deps):

    ```typescript
    interface AlertPayload {
      userId: string;
      projectId: string;
      ruleId: string;
      projectName: string;
      userEmail: string;
      periodLabel: string;
      spendUsd: number;
      limitUsd: number;
      percentUsed: number;
      action: string;
      slackWebhookUrl?: string | null;
    }
    ```

    Move the ENTIRE buildEmailHtml function from alertService.ts verbatim into emailTemplates.ts. Export it:
    `export function buildEmailHtml(payload: AlertPayload): string { ... }`

    Do not modify the function body — the HTML must remain identical.

    STEP 2 — Update lib/polling/alertService.ts

    Add import at the top of the file (after existing imports):
    `import { buildEmailHtml } from "./emailTemplates";`

    Remove the inline buildEmailHtml function definition from alertService.ts entirely (lines 102–133 in the current file).

    All other code in alertService.ts remains unchanged.
  </action>
  <verify>
    Run: npx tsc --noEmit
    Expected: no type errors. The import resolves and buildEmailHtml is used in sendEmail without errors.
  </verify>
  <done>
    emailTemplates.ts exports buildEmailHtml. alertService.ts imports it and no longer defines it inline. TypeScript compilation passes cleanly.
  </done>
</task>

</tasks>

<verification>
- [ ] lib/polling/emailTemplates.ts exists and exports buildEmailHtml
- [ ] alertService.ts imports buildEmailHtml from ./emailTemplates
- [ ] buildEmailHtml is NOT defined inline in alertService.ts
- [ ] npx tsc --noEmit passes
- [ ] AlertPayload interface in emailTemplates.ts matches the one in alertService.ts field-for-field
</verification>

<success_criteria>
emailTemplates.ts created. alertService.ts imports from it. TypeScript compiles without errors. No functional behaviour changes — email HTML is identical.
</success_criteria>

<output>
After completion, create `.planning/phases/06-email-alerts-slack/06-03-SUMMARY.md`
</output>
